Array  methods
----------------
slice // selects a part from an array between provided index
concat // Concat is used to merge two array
findIndex // findIndex is used to find the index of the asked element 
find // find is used to find the element present in an array
filter // filter is used to filter the element of an array from asked filters
map // map is used to create a new array where each element goes through a function called it is also used for formatting an array of Objects
splice // splice is basically used to insert or replace desired element in an array
push // push is used to add new element to an array at last
pop // pop is used to remove an element from the last


